{{yield}}
<img class="course-image" src="{{unbound model.image-info.square2x.url}}">
{{#link-to 'course' model.title}}
    <h2 class="course-title">
        {{unbound model.title}}
    </h2>
{{/link-to}}
<div class="course-summary">
    {{unbound model.summary}}
</div>